---

kanban-plugin: board

---

## Backlog

- [ ] **Image/photo input (multimodal).** Confirmed live (2026-08-16) via `/api/tags`: `qwen3.5:9b` and the saved `assistant:latest` (built on it) both report `vision` alongside `tools`/`thinking` in Ollama's capabilities — so the actual tool-calling dispatch model already supports image input, not just the 27B (which has vision but no tools). Nothing in the chat pipeline handles images at all yet (`MessageInput.tsx` is text-only, `types.ts`'s `Message` has no image field, `ollama.ts` doesn't send Ollama's `images: [base64...]` field) — this is a real, unscoped feature: file/paste attach in `MessageInput`, base64 encode, wire it through `WireMessage`, render a thumbnail in the message bubble. Not attempted yet, just confirmed feasible #phase3 #shell #harness
- [ ] **MCP registry-backed discovery.** Checked live (2026-08-16): neither `registry.modelcontextprotocol.io` (flat/unranked directory, no popularity API) nor mcpservers.org actually ranks by usage — an in-app "browse top servers" feature can't be built by querying either source, it'd have to be hand-curated and kept up to date manually. **Quick fix done instead** (see Done): a small hand-picked "Suggested" chip row (Filesystem/Blender/Obsidian) in Settings → Tools that prefills the add-server form. Real follow-up if this grows: expand the curated list (GitHub official server needs a PAT via env, now actually pluggable — see Done), or revisit a live-registry browser if either site ever adds a popularity/search API #phase4 #harness
- [ ] Font override: replace the free-text input with a dropdown of known-safe/pre-vetted font names. User-reported live: the text input won't accept spaces while typing (root cause not yet diagnosed — not the `FONT_RE` validation regex, which does allow spaces; possibly a webview/IME quirk), which breaks typing any multi-word font name (e.g. "M PLUS Rounded 1c") at all. User explicitly doesn't want free-typing to be the long-term UX anyway ("don't really want users to type anyway") — a dropdown sidesteps both the typing bug and the offline-font-availability problem (theme builder could ship a short vetted list, or eventually detect locally installed fonts) #phase3 #shell #idea
- [ ] Hotkeys — user floated wanting global keyboard shortcuts (e.g. toggle the widget without touching the tray icon). Not scoped yet #phase3 #shell #idea
- [ ] Deferred: optional Ollama companion commentary on the toy's parsed board (plain prompt, not tool-calling) #phase1 #harness #deferred
- [ ] Deferred: decide toy popup styling — companion-style vs. game-menu-style (or both as swappable themes) #phase1 #shell #deferred
- [ ] Working directory concept (fuller version): let a conversation be scoped to *any* project folder the user opens, not just this app's own repo. **The specific recurring gap this item was tracking is fixed** (2026-08-15, see Done): env context now includes this app's own `project_root`. What's still missing is the general case — a user pointing the model at a *different* project gets no equivalent grounding for that folder; would need an explicit per-conversation "working directory" the user sets #phase3 #harness #idea
- [ ] Better cross-conversation memory (like other tools the user has used) — user's own framing (2026-08-15): memory/RAG, likely a local vector store over past conversations + retrieved context folded into the system prompt at turn time, same general shape as this app's own auto-injected env-context block. **User confirmed (2026-08-16) no backend/vector-store infra exists yet** — this is greenfield, not "wire an existing thing in." Needs its own ideation pass before building: candidates worth comparing are an embedded local vector store (e.g. `sqlite-vec`/LanceDB — no separate server process, fits this app's local-only posture) vs. a real vector DB service (Qdrant/Chroma — more capable, adds an always-running dependency this app has avoided everywhere else, see loopx/WSL friction). Also needs an embedding model decision (Ollama serves embedding models too, e.g. `nomic-embed-text` — worth checking what's already pulled locally) and a retrieval trigger design (every turn? on-demand tool call the model invokes itself, same dispatch pattern as every other skill?). Still vague/future, explicitly deferred by user in favor of finishing the core first #phase4 #harness #idea
- [ ] File-organization use case (e.g. "sort my Downloads folder," "rename these by pattern") — would need a move/rename tool, a different risk tier than write_file since it operates on the user's existing files rather than ones the model created. Bulk operations would need real approval-UX thought (approve-all vs. per-file) before building, not just port the single-call pattern #phase4 #harness #idea
- [ ] Push toy task through to a finished artifact across multiple sessions #phase3 #harness
- [ ] More tools beyond read_file/write_file/list_directory — need-driven from actual toy/agent testing, not built speculatively #phase3 #harness
- [ ] Idea (not committed): a popout "run viewer" — separate small window showing currently-running loopx tasks live as they progress. Might belong to the harness/monitoring layer rather than the chat app itself; decide scope when it comes up. Note: current digest is a *snapshot* of todo state (counts, next queued item), not a true live "agent is doing X now" feed — that needs an actual autonomous loop running first, which doesn't exist yet #phase2 #shell #idea
- [ ] RAM/VRAM usage forecaster for the context-length slider — show estimated memory impact, not just the raw token count. Motivated by jumping context to 110k spiking GPU to 100% #phase3 #shell #idea
- [ ] Real Mica/Acrylic transparency still doesn't render despite 4 targeted fixes (apply_mica, transparent html/body, DwmExtendFrameIntoClientArea, and now SWP_FRAMECHANGED). **`SWP_FRAMECHANGED` tried and user-confirmed live it did not fix it** (2026-08-15: "it does look better [root-bg fix] but it's for sure not glass") — the frame-recompute-nudge hypothesis is now ruled out too, same as the WebView2-background lead before it. **The previously-logged "next lead" (WebView2's own compositor background) was ruled out by reading source, not by testing it**: wry 0.55.1 already calls `ICoreWebView2Controller2::SetDefaultBackgroundColor(0,0,0,0)` automatically whenever `tauri.conf.json` has `"transparent": true` (which this app's config already does) — that lever was never the blocker. Root-bg opaque fallback (see Done) makes the app look intentional and clean in the meantime regardless of whether Mica ever lands — theme packs stay deliberately built on top of it, not around it: all pack colors are translucent rgba() so any pack picks up the real glass backdrop the moment this resolves. **4 attempts down with no clear next lead identified** — worth treating as genuinely parked rather than reaching for a 5th guess next time; if revisited, start from a fresh web/community search for other Tauri+Mica-on-frameless-window reports rather than more local reasoning from the same 2 crates' source #phase3 #shell #investigate
- [ ] Digest view: more visual/graph-modular presentation instead of plain text cards — user feedback after seeing the first working version #phase2 #shell
- [ ] Window animation polish: smoother slide easing/duration, consider bottom-center "taskbar-style" popup position instead of bottom-right corner, more compact overall #phase3 #shell
- [ ] Stretch: point the harness at its own UI code as a second toy task #phase3 #stretch
- [ ] Image-generation backend (e.g. ComfyUI/SDXL) wired in as a second tool-dispatch target #phase4
- [ ] SillyTavern / companion presentation mode #phase4
- [ ] Messaging integrations / additional skills #phase4
- [ ] Packaged/friend-ready distribution: detect whether Ollama is running on launch and guide setup if not (install/start/pull a model), rather than assuming a dev environment #phase4 #shell #idea
- [ ] Model switching from within a saved custom model's conversation — user tried to switch models mid-conversation after using a saved custom model and couldn't. Related: the model name shown (`assistant:latest`) isn't a meaningful title either — worth a friendlier display name, separate from the underlying Ollama model tag #phase3 #shell
- [ ] MCP server discovery/browse — a reliable registry/directory of existing MCP servers to pick from, rather than the user hand-typing command/args for a server they already know about (see McpServers.tsx) #phase4 #harness #idea
- [ ] Titlebar duck mascot (walking/animated) — user wants to design this themselves outside the app; needs a spec doc (asset requirements: format, dimensions/aspect ratio for the ~28px titlebar, frame count/style if animated, how it'd integrate with the titlebar's existing icon buttons) handed to them to work from #phase3 #shell #idea


## Ready

- [ ] Theme packs: a "new pack" builder — currently importing requires hand-writing JSON (or asking a model for it); a small in-app form (color pickers per variable, name/author fields, export-to-file) would let a user author and share packs without knowing the format. **Now has a home**: Settings → Theme shows a placeholder card noting this is planned (2026-08-15), but the builder form itself still isn't built — unchanged in scope, just no longer homeless #phase3 #shell


## In Progress



## To Test

- [ ] Save as custom model (`/api/create`) — name a model, verify it appears in the picker and actually has the system prompt baked in
- [ ] Theme pack system + titlebar 🎨 picker: pick each built-in (Auto follows the OS; Light/Dark force it even against the OS setting; Psyduck Yellow tints everything amber), confirm the whole UI (titlebar, sidebar, bubbles, settings, digest, activity tracker) re-themes since it all reads the CSS variables. Import a pack via pasted JSON (and deliberately paste something invalid — should show a readable error, not break the UI), import a `.json` pack file, delete an imported pack (if it was active, should fall back to Auto). Reopen the app — selected theme and imported packs should persist. `tsc`/`cargo` clean per build, not yet manually re-verified. **Partially covered by fixes already landed** (2026-08-15, see Done): a swatch-backdrop compositing bug and a Psyduck Yellow accent-hue mismatch are fixed, and the missing `read_theme_pack` command behind "Import pack file…" is fixed — but delete/persistence/invalid-paste-error still need a live pass. Also now reachable a second way, Settings → Theme (same `ThemePickerMenu` component as the titlebar popover) — worth clicking through there too #phase3 #shell
- [ ] Custom instructions / system prompt (already built, per-conversation) — user hasn't tried it yet
- [ ] Chat titles — send a first message in a new chat, confirm the sidebar title updates automatically; double-click a title to confirm manual rename works
- [ ] Real bug fix: `MessageInput`'s textarea was only disabled by `!conversation.model`, never by `isStreaming` — the Send button correctly hides itself while streaming, but pressing Enter in the textarea bypassed that entirely, letting a new message fire a second, concurrent `sendMessage()` while a tool approval was still pending on the first turn. Symptom user hit directly: typed a correction ("we're working in D:, not C:") while an approval prompt was showing, sent it, and the stale prompt stayed stuck on screen since nothing ever resolved it. Fixed at both the entry point (`submit()` now no-ops while `isStreaming`) and defensively inside `useChat.ts::sendMessage` (early-returns if already streaming, regardless of caller). `tsc --noEmit` clean, pure frontend, hot-reloads — not yet manually re-verified #phase3 #shell
- [ ] Real filesystem context auto-injected into the system prompt (`get_environment_info` Rust command + `lib/environment.ts`): reports real OS + home/Documents/Desktop/Downloads paths, folded into every turn's system message ahead of the user's own system prompt. Built after a live test showed the model guessing a Linux-style `/home/...` path on Windows and looping through failed writes until hitting the 6-call safety cap. `cargo check` + `tsc --noEmit` clean; not yet exercised live #phase3 #harness #shell
- [ ] Real Ollama error detail surfaced in the UI instead of a bare status code: `streamChat` now reads and parses the response body on a failed request (`res.text()` → try JSON `error`/`error.message`, fall back to raw text) and appends a hint for 500s ("often means the conversation is too large for the context window..."). Also: `WireMessage` gained `tool_call_id`, which `toWireMessages` was silently dropping despite `Message.toolCallId` existing specifically for this — a real correctness gap vs. Ollama's tool-message wire format, fixed regardless of whether it was the actual cause of the incident below. `tsc --noEmit` clean, pure frontend — not yet manually re-verified #phase3 #shell
- [ ] Standing "converge quickly" instruction added to the auto-injected system context (same mechanism as the environment-context fix): after two separate real incidents (the duck SVG test and tonight's CSS restyle) where the model kept re-attempting the same kind of change speculatively and burned through most of the 6-call safety cap second-guessing an already-workable result, added `AGENT_BEHAVIOR_HINT` in `useChat.ts` — always included ahead of environment context and the user's own system prompt. `tsc --noEmit` clean, pure text/no new hooks (should hot-reload safely) — not yet manually re-verified #phase3 #harness
- [ ] Patch/diff-style `edit_file` tool — **user-confirmed live on a markdown file** (2026-08-15) after the Tauri argument-case fix (see Done); not yet tried on a code file, so still open whether the unique-match requirement holds up against denser/more-repetitive source text #phase3 #harness
- [ ] MCP connector support (see Done for what was built): stdio JSON-RPC client + Settings → Tools UI to add/connect/disconnect servers. `cargo test`/`cargo check`/`tsc --noEmit` all clean, not yet exercised live against a real MCP server (e.g. `npx -y @modelcontextprotocol/server-filesystem <path>`) #phase4 #harness
- [ ] Tool-approval prompt redesign + Stop button theme-color fix (see Done for both) — `tsc --noEmit` clean, hot-reloads, not yet manually re-verified across the different risk tiers (read/write/execute) and across themes #phase3 #shell
- [ ] Root-bg opaque fix + newest Mica attempt (see Done for both) — `cargo check`/`cargo test`/`tsc --noEmit`/`vite build` all clean. Root-bg fix needs a live look across all 4 built-in themes to confirm the grey/black bleed is actually gone; the Mica attempt (`force_dwm_frame_recalc`) needs a dev-server restart + look since source-reading can't confirm whether SWP_FRAMECHANGED actually made a visual difference #phase3 #shell
- [ ] Context-overflow trimming + tool-call-cap Continue button + empty-completion retry (see Done for full detail) — `npx vitest run` (9 tests), `tsc --noEmit`, `vite build`, and `cargo check` all pass. **First live test already ran and caught a real gap** (2026-08-15, user: "still broke context" on a single `read_file` call at the *default* `numCtx=8192`, not an edge case): the initial fix only retried on a *thrown* Ollama error, but the actual failure was a successful 200 OK stream containing only thinking tokens and no content — the model spent its whole remaining generation budget thinking and never got to an answer, which isn't a rejected-request shape at all. Fixed by also treating "ended with thinking but no content/tool calls" as retry-worthy, with a harder trim on that retry (0.25x vs. the error path's 0.5x) since the point is freeing generation headroom, not shrinking a rejected payload. **Still not proven to fully close this** — a model that wants to think long enough could in principle still exhaust even a maximally-trimmed request's headroom at a given `numCtx`; the fix meaningfully improves the odds and turns a silent dead end into one real retry + an honest terminal message, not a guarantee. User's own workaround in parallel: raising `numCtx` toward 100k for this 9B model, which the retry logic doesn't interfere with. Needs, against a real small model: (1) reproduce the original failure to confirm the retry now actually fires and either succeeds (look for the "trimmed to fit" note) or fails cleanly; (2) the original list still applies — a low `numCtx` + large `read_file` across several tool-calling turns, and temporarily lowering `MAX_TOOL_ITERATIONS` to 2 to test the Continue button #phase3 #harness
- [ ] Cody Duck promoted to the base Light/Dark themes (see Done) — **user-confirmed live** (2026-08-15): "otherwise it's great :)... getting there". Auto-mode/index.css sync specifically still not confirmed live (see caveat above) #phase3 #shell
- [ ] League Hextech rebuilt with real LoL client colors + explicit semantic tokens (see Done) — user reported the sidebar looked broken on the previous purple/orange version; rebuilt from scratch with authentic Hextech gold/navy and every semantic surface token explicitly set rather than relying on inheritance. `tsc --noEmit`/`vite build` clean, not yet visually re-verified live #phase3 #shell
- [ ] Tray-first window lifecycle (close-to-tray, widget/full modes, expand/collapse) — see Done for full detail. **First live test already caught two real bugs, both fixed same session**: (1) widget size was set via `PhysicalSize`, which on a DPI-scaled display shrinks the actual usable CSS pixel area well below the intended 420×620, causing the model picker and message input to visibly overflow the window — fixed by switching to `LogicalSize`; (2) the model-picker header didn't fit the compact widget at all and needed hiding there specifically, not just resizing. Needs, against the real app (native window behavior can't be verified via `cargo check` alone, same as every past window/Mica fix this session): confirm the ✕ button hides to tray rather than quitting; confirm Alt+F4 does the same; confirm the tray icon always reopens at the correct (now-fixed) widget size; confirm clicking away from the widget auto-hides it but doing the same in full mode does NOT; confirm expand/collapse resize+reposition correctly and the header show/hide follows compact mode correctly after this second pass #phase3 #shell
- [ ] Per-conversation settings (model info, system prompt, sampling params, save-as-custom-model) moved from a collapsible toggle in the chat header into Settings → General, under a new "Active conversation" section (see Done) — user's own motivation: actively testing the save-as-model flow to manage different personas/modelfiles, and the old inline toggle location didn't fit well with the widget/full-mode split anyway. Screenshot-confirmed live that the move itself works correctly (model info, system prompt, sliders all render under General as expected). Follow-up from that same screenshot: `.settings-menu__section`'s `max-width` was a fixed 480px, leaving most of the pane empty in full-mode's much wider window — widened to `min(720px, 100%)`, not yet re-verified live #phase3 #shell
- [ ] Settings menu reorganization (idea, not scoped) — user floated wanting to revisit the Settings tab structure again as more gets added (Theme/General/Tools today) — mentioned a possible dedicated MCP tab (separating it out of Tools) and a SillyTavern-style "Assistants"/persona tab, building on the "Active conversation" system-prompt section just moved into General #phase3 #shell #idea
- [ ] `.message__role` ("USER"/"ASSISTANT" label) bubble-text-color fix + real font-override control in Settings → Theme (see Done for both) — screenshot-confirmed the role label was missed in the bubble-text-color split above (still unset, inheriting the generic body color regardless of bubble). Also screenshot-confirmed the Google-Fonts-in-theme-packs limitation is a real, live problem, not just a documented caveat — user's fonts (Quicksand/M PLUS Rounded 1c) aren't installed and were silently falling back with no visibility into it. `tsc --noEmit`/`vitest`/`vite build` clean; neither fix re-verified live yet #phase3 #shell


## Blocked



## Done

- [ ] Real Windows toast notifications with inline Approve/Deny buttons (see Done for full detail) — `cargo check`/`tsc --noEmit`/`vitest`/`vite build` all clean, **completely unverified live** — this is native OS toast + WinRT action-button territory, nothing about whether it actually renders/fires correctly can be confirmed without literally seeing a toast appear and clicking a button on it. Needs, against the real app: (1) trigger a tool call while the window is hidden, confirm a toast appears with real Approve/Deny buttons (not just text); (2) click Approve, confirm the pending call actually proceeds — the event round-trip (Rust `emit` → JS `listen` → `approveToolCall()`) is unverified; (3) same for Deny; (4) confirm a plain "response ready" notification fires for a normal turn finishing while hidden, and that the **first run prompts for OS notification permission** (`isPermissionGranted`/`requestPermission` — Windows should auto-grant for a signed/installed app but behavior in an unpackaged dev build is genuinely unknown); (5) **expected, not a bug**: in `npm run tauri dev` the toast will likely show "Windows PowerShell" as the sender name/icon rather than "CodyChat" — this is `notify-rust`'s own documented dev-mode limitation (needs a real installed app with a registered AppUserModelID), not something to debug #phase3 #shell #harness
- [ ] ModelPicker no-longer-silently-swaps-model fix (see Done) — user-reported real bug (`docs/codyfinds.md`): a saved custom model "not finding itself" and appearing to have "lost its system prompt". `tsc --noEmit`/`vite build` clean; needs live verification specifically with the actual custom model the user originally lost, to confirm it now shows as "⚠ name (not found)" instead of silently vanishing, and that manually reselecting it (once Ollama actually has it) sticks #phase3 #shell
- [ ] Theme system evolution + Cody Duck theme + game-UI chrome pass (see Done for full detail) — `tsc --noEmit`/`vite build`/`vitest` all clean, not yet visually verified live. User's own framing going in: Cody Duck's palette "needs more fine tuning" — expect another iteration pass once actually seen. Also: the listed Google Fonts (Quicksand, M PLUS Rounded 1c, Nunito Sans, Fredoka, and Zen Maru Gothic/Kosugi Maru which weren't used in this pass) only render if installed locally — nothing is bundled, so verify what they actually look like on a machine that doesn't have them installed too, not just one that might #phase3 #shell
- [ ] Overall UI polish pass (scrollbars, focus rings, range sliders, message-bubble shadow) — `tsc --noEmit`/`vite build` clean, not yet manually re-verified live. **Turn-flow graph redesign is separately confirmed working** (2026-08-15, user: "Activity Log works great I like it") #phase3 #shell
- [ ] "Jump to latest" button: the scroll-fix follow-up — auto-follow and free manual scrolling were in real tension (scroll up to read while thinking → auto-follow correctly stays off → but then no easy way back to the live stream without dragging the scrollbar). Added the standard chat-app resolution: a small floating pill button appears whenever the user has scrolled away from the bottom (`!autoScroll`), regardless of whether a response is streaming; clicking it smooth-scrolls to bottom and re-arms auto-follow, same as sending a new message already does. `chat-window__messages` wrapped in a new relatively-positioned `chat-window__messages-wrap` so the button can float over it. `tsc --noEmit` clean, pure frontend, hot-reloads — not yet manually re-verified. **Note:** this edit collided with a concurrent dogfood `write_file` to the same file (see Done) — recovered via git, not lost, but the fix itself is otherwise unverified live #phase3 #shell
- [ ] Scrolling during streaming: the auto-scroll effect fired on every single streamed token/thinking-update and force-snapped back to the bottom, making it impossible to scroll up to read earlier messages while a response was still running — user-flagged after noticing it fighting a manual scroll-up mid-"thinking." Fixed with the standard chat-UI pattern: track whether the user is already near the bottom (`shouldAutoScrollRef`, ~60px threshold via an `onScroll` handler on the messages pane); only auto-follow new content if so; always re-enable on sending a new message or switching conversations. `tsc --noEmit` clean, pure frontend change (hot-reloads in the already-running dev instance, no rebuild needed) — not yet manually re-verified #phase3 #shell
- [x] Scaffold React + Vite + TS Ollama chat UI (`ui/`) #phase0
- [x] Set up docs/ Obsidian vault, root CLAUDE.md/AGENTS.md #phase0
- [x] Decide toy task: practice desktop utility app, Tauri/Rust, throwaway #decision #phase1
- [x] Install WSL2 Ubuntu + loopx (required: loopx needs `fcntl`, Unix-only, no native Windows support) #phase1 #harness #infra
- [x] Minimal Ollama client (`harness/ollama.py`, stdlib-only) #phase1 #harness
- [x] loopx wrapper (`harness/loopx_client.py`) + proven end-to-end: real Ollama call → todo complete → refresh-state → quota spend-slot, all from native Python shelling into WSL #phase1 #harness
- [x] Install Rust (rustup stable-msvc) + confirm MSVC C++ build tools present #phase1 #harness #infra
- [x] Scaffold Tauri/Rust project skeleton (`toys/kanban-reader/`, vanilla-ts, `cargo check` passes clean) #phase1 #harness
- [x] Run harness headless via CLI, building the toy utility, loopx tracking objective/todos/evidence #phase1 #harness
- [x] Parse `docs/Kanban.md` into structured board data, in Rust — `src-tauri/src/kanban.rs`, 2 unit tests (synthetic fixtures) + 1 integration test against the real live file (5 lanes, 28 cards) all pass #phase1 #harness
- [x] Plain proof-of-parse render wired end-to-end (`read_kanban_board` Tauri command → `src/main.ts`), app builds and runs — unstyled by design, real styling is its own card below #phase1 #harness
- [x] Tauri app skeleton for the *real* shell: added `src-tauri/` to existing `ui/` via `tauri init` (not a new project — `ui/`'s components are the content pane, no porting needed), `cargo check` clean, full build runs the real chat UI in the webview, confirmed against real Ollama #phase1 #shell
- [x] Window chrome: frameless window, tray icon (Show/Hide, Pop to Corner, Always on Top toggle, Quit), corner-pop positioning, drag-to-move — including catching and fixing a real bug where dragging silently failed for lack of the `core:window:allow-start-dragging` capability permission #phase1 #shell
- [x] Collapsible sidebar (titlebar toggle) + wider default window (380x560 → 460x640) so the layout doesn't smoosh in the compact widget window — found via live user testing #phase1 #shell
- [ ] Split message-bubble text color (`--user-bubble-text`/`--assistant-bubble-text`, see Done) — user-driven fix while editing the Dark pack's `user-bubble` themselves (changed it to a near-white fill to differentiate from the assistant bubble) and noticing the shared `--text-h` (tuned for dark surfaces) went illegible against it. `tsc --noEmit`/`vitest`/`vite build` clean, not yet visually re-verified live #phase3 #shell
- [x] Basic IPC scaffolding: `get_app_info` Tauri command (Rust) → React titlebar, real round-trip confirmed live (not a stub) #phase1 #shell
- [x] Wire loopx-driven state into the Tauri shell + render live digest (newspaper/blog-style cards): `get_loopx_digest` Tauri command shells directly into WSL (no separate server — pivoted off an initial HTTP-server design per user feedback), `LoopxDigest.tsx` polls it every 8s, toggled via titlebar button. User-confirmed twice against real data #phase2 #harness #shell
- [x] Skill/tool-dispatch: `ollama.py::chat()` (tools-capable), `skills.py` (registry + `dispatch()` gated behind explicit confirm), first skill `read_file`, model=`qwen3.5:9b` (not the 27B). Proved live both ways: real prompt → model calls the right skill → confirmed → real result used in the final answer; and confirm=False verified to actually block on denial — not just look like it does #phase2 #harness
- [x] Chat UI: streamed thinking tokens + collapsible "thought process" breakout per message. Found + fixed a real bug along the way (via user report + live diagnosis, not guessed): a response could hit the context limit while still "thinking" and never produce content, leaving the UI showing "Thinking…" forever with no way to tell it was actually done. Fixed the UI state (only the actively-streaming message shows "Thinking…"; a finished-with-no-content response now shows a clear message) and bumped default context 4096→8192 for new conversations #phase3 #shell
- [x] Settings: real model info via `/api/show` (param size, quantization, capability badges), context-length slider now tracks the model's actual max instead of a fixed 32768, clamps saved value on model switch. Turned up a bonus finding: `/api/show` shows `tools` in the 27B's capabilities where `/api/tags` didn't — resolves the earlier tracked wrinkle as endpoint inconsistency, not a real gap #phase3 #shell
- [x] `/api/create` custom model creation: Settings "Save as custom model" (name + system prompt), disabled until both exist, sanitizes the name, `ModelPicker` refreshes automatically after a successful save. Verified `/api/create` is near-instant (no weight copy, just layers a system prompt) via a real create+verify+delete cycle before writing code #phase3 #shell
- [x] Real Markdown rendering: `react-markdown` (renders to React elements, no `dangerouslySetInnerHTML`/sanitization needed) + `remark-breaks` (single newlines render as line breaks, matching how models actually format vs strict CommonMark). Headers, lists, bold/italic, links, blockquotes, and inline-vs-fenced code all styled. User-confirmed live, including correctly distinguishing fenced code blocks from inline markdown formatting #phase3 #shell
- [x] Chat titles: auto-generated from the first message (whitespace-collapsed, truncated to 48 chars) when a new chat's title is still the default, double-click any sidebar title to rename manually (inline input, Enter/blur commits, Escape cancels) #phase3 #shell
- [x] Glass styling — partial. Working, user-confirmed: half-screen default size, slide-in/out animation, corner-pop positioning (including a real bug found+fixed: window ran under the taskbar because `monitor.size()`/`.position()` cover the full monitor, not the taskbar-excluded work area — switched to `monitor.work_area()` throughout). Not working despite 3 targeted source-verified fixes: real Mica/Acrylic transparency — moved to Backlog as an open investigation rather than left silently incomplete #phase3 #shell
- [x] **Live tool-dispatch in the actual chat UI** — the big one: ported skills to Rust (`skills.rs`: `read_file`/`write_file`/`list_directory`/`get_tool_definitions`, single source of truth for the JSON schemas), wired `tools` + streamed `tool_calls` detection into `streamChat`, rebuilt `useChat` as a real agentic loop (bounded to 6 iterations) with wire-format history reconstruction for tool-call/tool-result replay, built a real in-app approval UI (`ToolApprovalPrompt`, inline Approve/Deny — not a terminal prompt), and `MessageBubble` renders tool-call chips + tool-result bubbles. **Proven fully end-to-end by the user on the first real try**: asked the model to write a file, approval prompt showed real args, approved, and the file was actually created in the correct folder. This is the actual "write files with approval, like Claude Code" capability the user asked for #phase3 #harness #shell
- [x] First real dogfooding test: pointed `qwen3.5:9b` at `docs/MEMORY.md` + `docs/Kanban.md` (real multi-file `read_file` calls, verified against the actual file contents — not hallucinated) and had it write an accurate session summary to a new file, `docs/Session-Summary-2026-08-14.md`. First attempt failed — model read both files correctly but then asked a clarifying question instead of writing, likely because a big document dump (one literally titled "Kanban") triggered a generic "PM assistant" response pattern rather than continuing the task. Retried with a more imperative final instruction ("now call write_file..."), which completed the full read→read→write chain correctly. Real finding: tool dispatch was never the problem — small-model instruction-following on a compound multi-step request was #phase3 #harness
- [x] Live tool-call activity tracker + `list_directory` live: user-confirmed via screenshot on a second dogfooding turn (model exploring `ui/src-tauri/` itself — `list_directory` on the icons folder and the src-tauri root, `read_file` on `tauri.conf.json`/`package.json`/`Cargo.toml`) — `ActivityTracker.tsx` showed the live per-call checklist (checkmarks on completed calls, pause icon on the in-flight one) exactly as designed, and the polished `ToolApprovalPrompt` showed the summarized-args view with "Show full arguments" collapsed by default. All four skills (`read_file`/`write_file`/`list_directory` + the activity tracker/approval polish) are now proven live, not just compiled clean #phase3 #harness #shell
- [x] Persistent activity log: user-confirmed live — hit the 6-call safety cap asking for a logo (no image-gen tool; model overthought writing raw SVG in a text tool) and the stopped/final message correctly showed the collapsed "Activity log" breakout with the full step history, instead of the checklist just vanishing like before #phase3 #shell
- [x] Diagnosed a real "500 Internal Server Error" mid-dogfood (ActivityTracker.tsx restyle test, reading App.css). Ollama's server.log showed the actual cause: `srv operator(): got exception... Jinja Exception: No user query found in messages` — a chat-template validation failure, not a generic Ollama flake. Root-caused via evidence, not guessing: the request right before the failure was already at `n_tokens = 6386` of `n_ctx_slot = 8192` (78% full, `n_keep = 4`); the next request's added content (a large file read) plausibly pushed it over, and Ollama's context-fitting trim apparently dropped the original user turn before handing messages to the template — same root cause class as the earlier "stuck on Thinking forever" bug (context overflow), just failing loudly this time instead of silently. Ruled out an initial hypothesis (missing `tool_call_id` in the wire format) by reproducing several close variants via direct `curl` against Ollama (sequential rounds, deep rounds, parallel calls) — none reproduced the 500, so the real explanation is context size, not message shape. Fixed anyway as a real correctness gap (see To Test), plus made `streamChat` surface Ollama's actual error text on failure instead of a bare status code #phase3 #harness
- [x] **`ui/src/` dogfooding test succeeded — and caught a real write collision live.** After the context-overflow fix, retried the ActivityTracker.tsx restyle in a fresh conversation: the model produced a genuinely good redesign — a vertical timeline with connector lines between steps (colored by status), a pulsing accent ring on the in-progress step, a dashed ring for "awaiting approval," `prefers-reduced-motion` support, and `role="list"`/`role="listitem"`/`aria-label` accessibility on the component — while correctly preserving the `STATUS_ICON` export and `steps` prop exactly as instructed, and correctly leaving the `.activity-tracker--log` (persisted-log) modifier intact. **Real finding, not manufactured**: its `write_file` for `App.css` landed *while* a separate concurrent edit (the "jump to latest" button CSS) was also being made to the same file — a genuine full-overwrite race, exactly the risk flagged when `write_file`'s lack of patch/diff semantics was first called out. The concurrent edit was silently dropped by the model's overwrite. **Caught and fixed cleanly because git now exists**: `git diff` showed exactly what was lost, the dropped rules were reapplied on top of the model's redesign (not reverting its work), and `tsc --noEmit` confirmed everything still compiles together. First real, concrete evidence (not just a predicted risk) that a patch/diff-style edit tool is needed before dogfooding gets more concurrent/ambitious #phase3 #harness #shell
- [x] Git initialized at the project root — first commit made (119 files: `ui/`, `toys/kanban-reader/`, `harness/`, `docs/`, root instructions). Caught and fixed a real gap before committing: `ui/.gitignore` was missing the `.loopx/`/`.codex/goals/` exclusion that `harness/.gitignore` and `toys/kanban-reader/.gitignore` already had (this was the long-standing housekeeping backlog item — never actually done for the main project). Also added a root `.gitignore` for `.claude/settings.local.json` (personal tool-permission grants, not meant to be shared). Verified `node_modules`/Rust `target/`/loopx state were all correctly excluded before staging, by inspecting the full staged file list rather than trusting the gitignore blindly. Gives a real rollback point before dogfooding starts writing to files the model didn't create #housekeeping
- [x] Third `ui/src/` dogfood test: turned the persisted "Activity log" from a flat list into an inline compact flow-graph (nodes on a 3-column CSS grid, connectors snaking node-to-node, tinted by status), scoped correctly to the ~75%-wide message bubble per the brief, reusing the existing `ActivityStep` data and preserving the `STATUS_ICON`/`steps` contract. **Converged in 8 steps this time — did not hit the 6-call safety cap**, a real (if single) data point that the new "converge quickly" instruction may be helping, separate from thinking-phase duration (still long) which is more a model/thinking-mode trait than a harness lever. Confirmed via Ollama's server log that this ran on the 27B (`Qwen3.8-27B-GGUF:Q4_0`), the documented "heavier reasoning" model — not the 9B. Model honestly flagged its own limitation (long labels shift the connector elbow, CSS grid can't dynamically re-route) rather than claiming it was perfect, and proposed two real fixes. No write collision this time (nothing else was concurrently editing) #phase3 #harness #shell
- [x] Activity-log cleanup, done directly (not dogfooded) since the issue was already fully diagnosed. Turned out bigger than expected: a **fourth**, undiscussed dogfood iteration (9B, expandable per-step detail) had changed the step markup shape (`activity-tracker__step-wrapper`/`__step-title` instead of the third test's `activity-tracker__step`) without updating or removing the third test's connector-line/3-column-grid CSS — leaving ~140 lines of dead CSS silently targeting DOM nodes that no longer existed, so the flow-graph visual from test three had already been orphaned in both the live tracker and the persisted log, not just the log alone. Also confirmed: `MessageBubble.tsx` had a full duplicate of the step component and `STATUS_ICON` (drifted from the real one — unstyled classes, dropped `argsSummary`), and `.message__thinking-content` had never gotten CSS after a class rename left `.message__thinking` orphaned too. **Fixed**: `ActivityTracker.tsx`'s step component now exported and reused directly by `MessageBubble.tsx` instead of a second copy; replaced the dead connector/grid CSS with rules that target the actual current markup, including restoring status-based coloring (pulsing accent for running, danger border for error/denied, dashed for pending-approval) applied to the new step-title shape; restored `argsSummary` in the expanded view; renamed the orphaned `.message__thinking` CSS to match its actual class name; replaced several inline `style={{...}}` word-break hacks with proper `overflow-wrap: anywhere` on the relevant classes. `tsc --noEmit` clean, grepped for any remaining references to removed classes/components (none found). **Mid-fix correction from the user (screenshot-confirmed):** the outer collapsible "Activity log (N steps)" wrapper had also been dropped entirely during the fourth dogfood test — steps rendered bare with no summary toggle. Restored it: `ActivityLogBreakout` now wraps the per-step list in the same collapsed-by-default chevron pattern used elsewhere (matching the thinking breakout), nesting the newer per-step detail toggles inside rather than replacing the outer summary #phase3 #shell
- [x] Theme pack system + titlebar 🎨 picker (user request: "style this chat a bit better... with some theme pickers... an options menu sort of like the show tasks toggle... make it customizable with packs (so a user could load one if someone else creates one)" — first pack: Psyduck Yellow). Built as a **pack system, not a hardcoded theme list**: `lib/themes.ts` defines a pack as a named set of values for the CSS custom properties the whole UI already consumes (every component reads `var(--…)` and never hardcodes a color, so a complete reskin = a new palette, zero component changes). Packs are shareable by design — the file IS the JSON object (`{ id, name, author?, description?, scheme?, vars }`); import via pasted JSON or a pack file read through a new `read_theme_pack` Tauri command (plain `fs::read_to_string` in `commands.rs` + 2 unit tests — content is validated on the JS side before CSS injection, not Rust). **Security posture**: pack files are untrusted input and applying one means injecting CSS text, so `sanitizeThemePack` regex-checks ids (they end up in an attribute selector), var names (CSS identifiers), and requires plain color literals for every value — invalid entries are skipped (readable error when nothing usable remains), never injected; unknown `vars` keys are skipped rather than failed (forward-compat: a pack can introduce a variable a future component uses). **Built-in packs** in TS (so the picker can render swatches/blurbs): `auto` (removes `data-theme`, prefers-color-scheme decides — today's behavior), `light`/`dark` (mirror the index.css base/system-dark palettes — noted in-file to keep in sync if index.css is restyled), `psyduck-yellow` (first accent-variant pack; **all values deliberately kept translucent rgba() so packs pick up the Mica glass the moment that Backlog investigation resolves** — the user confirmed "the glassy theme isn't really working yet but that's the goal down the line"). **Applying**: `data-theme` on `<html>` + a persistent `<style>` element injecting vars scoped to `:root[data-theme="…"]` (beats both the base palette and the system-dark override, which was scoped to `:root:not([data-theme])` in index.css so it only governs `auto`); per-pack `color-scheme` so scrollbars match. **`ThemePicker.tsx`**: 🎨 titlebar button next to the ☰/⚙ toggles (with `app-region: no-drag` — the titlebar is a drag region and the popover is inside it), popover with swatch rows (auto gets a split light/dark swatch), imported-packs section with per-pack delete (deleting the active one falls back to auto), paste-JSON import, file import; outside-click/Escape closes. **Persistence**: `ollama-ui:active-theme` + `ollama-ui:themes` (same localStorage pattern as base-url/sidebar-collapsed); a saved id that no longer resolves falls back to auto rather than stranding the app unstyled. Theme is app-level, deliberately not in the per-conversation SettingsPanel. Pure frontend + one small Rust read command; needs `tsc`/`cargo` run + live verification (see To Test) #phase3 #shell
- [x] Theme picker color fixes: a swatch-backdrop compositing bug (a pack's swatch preview composited over the *currently active* theme's menu background instead of its own, so e.g. Light's swatch read muddy while Dark was active) and a Psyduck Yellow accent-hue mismatch, both fixed and verified. Documented here since neither was tracked as its own Kanban item when fixed. Doesn't fully close the "Theme pack system + titlebar picker" To Test item above — import/delete/persistence still need a live pass #phase3 #shell
- [x] Fixed a real gap: `read_theme_pack` (the Tauri command `lib/themes.ts::readThemePackFile` calls for "Import pack file…") didn't actually exist in the Rust backend — never registered in `lib.rs`, not defined in `commands.rs` or `skills.rs` — despite the theme-pack Done entry above claiming it was built. Likely lost or never committed. Added `read_theme_pack` (plain `fs::read_to_string`, no validation — `sanitizeThemePack` on the JS side is still the only gate before anything is injected as CSS) + 2 unit tests, registered in `lib.rs`. `cargo test` passes both; live click-through of "Import pack file…" still pending #phase3 #shell
- [x] Fixed the recurring env-context gap tracked in Backlog above: `get_environment_info` now also reports `project_root` (this app's own repo root), folded into the same auto-injected system-prompt block as the OS folder facts. Resolved via `env!("CARGO_MANIFEST_DIR")` at *compile* time (two levels up from `ui/src-tauri`) — correct for how the app is actually used (built and run on the same dev machine) but a **compile-time fact, not a runtime lookup**: goes stale if the repo is moved again without a rebuild, same class of staleness as any other compile-time path baked into a binary. Doesn't close the fuller "working directory concept" backlog item (arbitrary per-conversation project folders) — only fixes this app's own project root. 3 Rust unit tests pass, including one that structurally verifies the resolved path actually contains `ui/`/`docs/` rather than asserting a hardcoded string, so the test itself won't go stale on the next rename; `tsc`/`cargo check` clean #phase3 #harness
- [x] Search/grep tool (`search_files`): grep-like recursive regex search under a directory (case-insensitive by default, optional `*.ext`/substring filename filter), skips build-noise dirs (`node_modules`, `target`, `.git`, etc.) and binary files, capped at 200/1000 results and 20k files scanned. Solves the actual backlog motivation — locating something across a codebase without paying `read_file`'s 200KB-per-file cost on every candidate. `cargo check`/`tsc --noEmit` clean; not yet exercised live through the real app (only structurally verified via a browser-only dev-server preview, which can't reach Tauri's `invoke()`) #phase3 #harness
- [x] Shell/command execution tool (`execute_command`): runs via `cmd /C` on Windows, captures stdout/stderr on background threads (avoids a pipe-deadlock that reading only after process exit risks), 30s timeout + kill, output truncated past 100KB. **Approval-flow decision**: no new gating was needed — every tool call, including this one, already routes through the same per-call approval prompt (`requestApproval` in `useChat.ts`) that predates this tool, so shell commands require the same explicit Approve/Deny click as `read_file` does, by default. Added on top: a risk badge (Read-only/Write/Execute) on the approval prompt and in the new Settings → Tools list, with Execute getting a danger-tinted border, so a shell approval is visually distinct before the user clicks anything. Not yet exercised live #phase3 #harness
- [x] Settings menu (`SettingsMenu.tsx`) — not an existing Kanban item, net-new: a ⚙ titlebar button (freed up from the old, misleadingly-named "digest" toggle, which moved to 📋) opens a full settings view in place of the chat pane, with Theme / General / Tools tabs. Theme tab embeds the existing picker (`ThemePickerMenu`, factored out of the titlebar popover so both share one component) plus the pack-builder placeholder (see Ready). General tab holds the Ollama base URL, moved here from the per-conversation `SettingsPanel` since it was never actually per-conversation. Tools tab lists every tool (descriptions pulled from the same Rust schema Ollama uses, not duplicated) with a risk badge and an enable/disable toggle — disabling a tool strips it from what's sent to Ollama entirely, not just denies it at approval time; persisted in `localStorage`, takes effect on the next message without restarting. `tsc --noEmit` clean; tab switching and layout verified via dev-server preview, `invoke()`-backed content (tool list, base URL round-trip) not yet exercised live #phase3 #shell
- [x] Patch/diff-style edit tool (`edit_file`) — closes the item that had a real, concrete motivation: `write_file`'s full-overwrite semantics had already silently dropped a concurrent unrelated edit to `App.css` during a dogfood turn (recovered only because git existed). Built with Claude Code's own `Edit` tool shape rather than a unified-diff format on purpose — old_string/new_string find-replace is far more reliable for a small dispatch model to produce correctly than diff hunks with line numbers, which drift constantly. `edit_file(path, old_string, new_string, replace_all?)`: reads the file's *current* on-disk content at call time (not anything cached from earlier in the conversation), requires old_string to match exactly and uniquely unless `replace_all` is set, errors loudly (no match / ambiguous match) rather than guessing. `write_file`'s tool description now explicitly nudges the model toward `edit_file` for existing files. 4 new Rust unit tests (unique match, not-found error, ambiguous-without-replace_all error, replace_all) all pass; `cargo check`/`tsc --noEmit` clean #phase3 #harness
- [x] Real bug caught on the model's first live `edit_file` attempt: the call failed with `"command edit_file missing required key oldString"`. Root cause: Tauri v2's `#[tauri::command]` macro matches incoming JS argument keys as **camelCase** against the Rust parameter names by default — so `old_string` (the Rust param, and the name used in the JSON tool schema sent to Ollama and in the model's own tool-call arguments) only matched an `oldString` key, which nothing ever sent. This wasn't `edit_file`-specific: `search_files` (`file_glob`/`case_sensitive`/`max_results`) and `execute_command` (`working_dir`) have the identical latent bug — every multi-word Tauri command parameter in this codebase was affected, silently, since neither had been exercised live yet. Fixed by adding `#[tauri::command(rename_all = "snake_case")]` to `edit_file`, `search_files`, `execute_command`, and the new `mcp_call_tool` (see below) — keeps Rust's idiomatic snake_case naming and the existing JSON tool schemas unchanged, just opts these commands out of the camelCase default. `cargo check` clean #phase3 #harness
- [x] MCP connector support — real protocol work, not a placeholder. Hand-rolled JSON-RPC 2.0 stdio client (`src-tauri/src/mcp.rs`), matching this project's existing posture of hand-rolling protocol clients rather than pulling in an SDK (loopx_client.py, ollama.py, kanban.rs are all the same pattern) — the MCP stdio transport is genuinely simple (one JSON-RPC message per line, no extra framing). `mcp_connect(id, command, args)` spawns the server process (routed through `cmd /C` on Windows, same fix `execute_command` already needed for `npx`-style .cmd shims), performs the `initialize`/`notifications/initialized` handshake, calls `tools/list`, and keeps the connection alive in Tauri-managed state (`Arc<McpConnection>` per server id, background reader thread correlating JSON-RPC responses to pending requests via a channel map, stderr drained into a capped buffer surfaced on connect failure for actual diagnosability). `mcp_call_tool(id, tool_name, arguments)` and `mcp_disconnect(id)` round out the lifecycle. **Frontend**: `lib/mcp.ts` persists server configs (name/command/args) in localStorage like every other app-level setting; `hooks/useMcpServers.ts` owns live connection state and exposes `mcpToolDefs` — MCP tools qualified as `mcp__<serverId>__<toolName>` (Claude's own MCP naming convention) so `skills.ts::executeSkill` can route a call back to the right server without a second lookup table, merged into what `useChat` sends to Ollama alongside the built-in skills. **`McpServers.tsx`** (Settings → Tools) replaces the old placeholder: add a server (name/command/space-separated args), see live status (Disconnected/Connecting/Connected with tool count/Error with the actual stderr-informed message), per-server Connect/Disconnect/Remove. **Deliberate v1 scope decision: manual connect only, no auto-connect on app launch** — these are arbitrary external processes the user configures, and silently spawning them before Settings is ever opened is a worse default than an explicit click; config still persists across restarts, only the live connection doesn't. 4 new Rust unit tests (pure JSON-parsing helpers: tools/list parsing, text-content extraction) pass; `cargo check`/`tsc --noEmit` clean #phase4 #harness #shell
- [x] Found and fixed a real theme color bug: the Send/Stop button's "Stop" state had a hardcoded `rgba(209, 67, 91, 0.1)` background — a copy of the *light* theme's `--danger` value baked in as a literal, while its border correctly used `var(--danger)`. In every other theme (Dark, Psyduck Yellow, any imported pack), the border tracked the active theme but the background stayed stuck on light-theme red — a real per-pack mismatch, not a missing variable. Swept the rest of `App.css` for the same class of bug (any hardcoded hex/rgba not going through a CSS var) and found nothing else — the one other hardcoded-color spot (`.theme-picker__swatch--auto`'s split gradient) is intentional, representing "both light and dark" for a pack that has no single palette. Fixed with `color-mix(in srgb, var(--danger) 10%, transparent)`, matching the pattern already used elsewhere in the file (tool-approval's execute-risk tint, risk badges) #phase3 #shell
- [x] Tool-approval prompt redesign (`ToolApprovalPrompt.tsx`) — user asked for the approve/deny UI to feel cleaner. Previously a fully vertical stack: risk badge on its own line, then "Model wants to run X" + args below, then a right-aligned Approve/Deny row at the bottom — three stacked rows even for a no-args call. Restructured into a single header row (badge + tool name on the left, Approve/Deny immediately to the right) with the args summary/expand toggle collapsed into a body section below that only renders at all when the call actually has arguments. `tsc --noEmit` clean, pure CSS/JSX, not yet manually re-verified across risk tiers or themes #phase3 #shell
- [x] Overall UI polish pass — user feedback that the app still "feels old" by today's standards independent of the Mica issue. Scoped as a polish pass within the existing frosted-glass paradigm, not a redesign. `index.css`: custom slim scrollbars (webkit) replacing default OS scrollbars, one global `:focus-visible` ring for every interactive element (most controls had no focus state at all), a global `button` base adding consistent transitions + a subtle press-down (`translateY(1px)`) micro-interaction, a focus style for text inputs/textareas/selects (accent border + soft glow, matching contemporary form conventions), and fully custom `input[type=range]` styling (webkit + moz) — the temperature/top-P/context-length sliders in Settings were completely unstyled raw OS defaults before this, arguably the single most dated-looking control in the app. `App.css`: subtle box-shadow on message bubbles (previously perfectly flat), plus hover states added to several controls that had none at all (`model-picker`, `chat-window__settings-toggle`, and the primary accent-colored action buttons — sidebar "New chat", message send/stop, tool-approval approve/deny, MCP server add/remove). `tsc --noEmit` clean and a full `vite build` succeeds (catches CSS syntax errors tsc can't) #phase3 #shell
- [x] Graphical turn-flow view (`ActivityTracker.tsx::TurnFlowGraph`) — replaces the flat vertical "Activity log" checklist with a horizontal chain of connected nodes (thinking → tool call → tool call → … ), reusing the exact same underlying data (`ActivityStep[]` + the message's own `thinking`) with no new data needed, per the original backlog framing. Nodes are pill-shaped buttons (icon + tool name), connected by `→` connectors, wrapping onto more rows as needed rather than a fixed-width graph layout — deliberately not a full diagramming engine, matching the "same footprint as today's activity log" scope decision. Clicking a node shows its detail (args/result, or the full thinking text) in a single shared panel below the chain, replacing the old model where every step could be independently expanded inline. **Ordering decision**: the final message's own `thinking` is placed *last* in the chain, not first — it's chronologically the reasoning that happens after every tool call in the turn (once results are already in hand), right before the answer, not before the first tool call. **Two variants, one component** (avoiding the exact "second copy drifted out of sync" mistake logged earlier for the old `ActivityTrackerStep`): `variant="live"` keeps the bordered/backgrounded panel chrome below the message list and auto-follows the newest node while streaming; `variant="log"` has no chrome of its own (the message bubble provides it) and starts with nothing selected, inside the same collapsed-by-default "Activity log (N steps)" outer toggle as before. **Fixed a real duplication this surfaced**: a message with tool calls previously showed its final `thinking` in *two* places — the standalone "Thought process" toggle above, and (if it were added naively) the same text inside the flow graph. Resolved by only showing the standalone toggle when a message has *no* activity steps at all; turns with tool calls fold their final thinking into the graph instead. All dead `.activity-tracker*` CSS (~140 lines) removed in the same change rather than left orphaned — this exact class of mistake (restyling a component's markup without removing the old rules targeting it) was already logged once before as a real bug. `tsc --noEmit` clean, full `vite build` succeeds. **User-confirmed live** (2026-08-15): "Activity Log works great I like it" #phase3 #shell
- [x] Root-bg opaque fix — user-reported live (screenshot, 2026-08-15) after a dev-server restart: "background is still grey" and buttons not matching the theme, even after the Psyduck alpha bump above. Real root cause, bigger than Psyduck: `html`/`body`/`#root` have been set to fully `background: transparent` this whole time, on the assumption Mica would composite a real backdrop behind that transparency — it never has (see the still-open Mica investigation), so every theme (not just Psyduck) was rendering panels over raw black/grey nothing, not a colored backdrop. Fixed by adding `--root-bg`, a genuinely solid (alpha 1) token kept **separate** from the translucent `--bg`/`--bg-alt`/etc. panel colors, which stay untouched and glass-ready — `html/body/#root`'s background switched from `transparent` to `var(--root-bg)`. Added to the base `:root`, the system-dark media block, and all 3 built-in packs that force a look (`light`/`dark`/`psyduck-yellow`); `auto` needs no override since it already inherits correctly via the same cascade every other one of its variables uses. One-line revert (`transparent` back) whenever Mica is actually confirmed compositing. `cargo check`/`tsc --noEmit`/`vite build` all clean; not yet visually re-verified across all 4 themes #phase3 #shell
- [x] Newest Mica attempt: `force_dwm_frame_recalc()` in `lib.rs`, called right after `apply_mica`/`apply_acrylic`. Before touching new code, **ruled out** the previously-logged "next lead" rather than assuming it — read wry 0.55.1's own source (`src/webview2/mod.rs`) and confirmed it already calls `ICoreWebView2ControllerOptions3::SetDefaultBackgroundColor(0,0,0,0)` automatically whenever `tauri.conf.json` sets `"transparent": true` on the window (which this app's config already does) — so WebView2's own compositor background was never the blocker, that lever is already pulled. Also read `window-vibrancy` 0.6.0's actual Windows implementation directly rather than assuming it was correct — `apply_mica` is a standard, textbook-correct `DwmSetWindowAttribute(DWMWA_SYSTEMBACKDROP_TYPE, DWMSBT_MAINWINDOW)` call, nothing wrong there either. **New hypothesis**: `DwmSetWindowAttribute` only *requests* the backdrop type — on a window that's already created and mapped (not hidden at creation), DWM doesn't reliably repaint its non-client frame just because the attribute changed after the fact. `SetWindowPos(hwnd, None, 0,0,0,0, SWP_FRAMECHANGED | SWP_NOMOVE | SWP_NOSIZE | SWP_NOZORDER | SWP_NOACTIVATE)` is the documented way to force that recompute without actually moving/resizing/reordering/refocusing the window — added right after the mica/acrylic call. Needed adding `Win32_UI_WindowsAndMessaging` to the `windows` crate's feature list in `Cargo.toml`. `cargo check`/`cargo test` (11 tests) clean. **Cannot be confirmed from source alone** — needs a real dev-server restart and a look, same as every other native-window fix in this investigation's history #phase3 #shell #investigate
- [x] **Small-model reliability: context-overflow trimming + tool-call-cap Continue button.** User-directed via a full plan-mode pass (explore → design → review → approved plan at `C:\Users\ZBT_C\.claude\plans\a-few-things-i-logical-melody.md`) before any code was written, since this touched the core agent loop. **Ruled out `harness/`/loopx as the right place first**: confirmed the Python tool-dispatch prototype there was already superseded by the live Rust/TS implementation (2026-08-14 decision), and loopx only governs the unrelated project-todo digest feature — zero involvement in the chat/tool-calling path. The fix lives entirely in `ui/src/`.
	
	  **Root cause, confirmed by reading the code**: `useChat.ts` sent the *entire* conversation history verbatim on every turn with zero trimming — `read_file` alone truncates to 200KB of raw text per call (tens of thousands of tokens), easily exceeding the default 8192-token `numCtx` in a single tool result, and nothing ever shrank history afterward. A second confirmed bug found along the way: Ollama's mid-stream `chunk.error` path (`ollama.ts`) got no context-overflow hint at all, only the initial-500-response path did — a real asymmetry, not by design.
	
	  **`ui/src/lib/contextBudget.ts`** (new, pure functions, no Tauri/fetch dependency): `estimateTokens` (chars/4 heuristic — no tokenizer dependency added), `budgetTokensFor(numCtx)` (reserves 25%/512-token generation headroom), `trimMessagesToBudget(msgs, budget)` — three passes: (1) cap any single oversized `role: 'tool'` message on its own; (2) collapse older tool results to short placeholders, oldest first, keeping the 2 most recent full; (3) fallback — collapse whole oldest plain-content messages. Never touches the most recent user message. **A correctness bug caught and fixed during implementation, not just planning**: an early draft of pass 3 stripped `toolCalls` from old assistant messages too, which would leave a paired tool-result message's `tool_call_id` referencing a call no longer in history — an invalid wire payload, not just an aggressive trim. Fixed by having pass 3 skip any message with `toolCalls`. Trimming only affects the outbound wire payload — `conversation.messages` (what's actually displayed) is never mutated, so the user can always scroll up and see the real, untrimmed tool output.
	
	  **`ui/src/lib/ollama.ts`**: `OllamaError` gained `status`/`likelyContextOverflow` fields; the existing 500-response hint logic now also applies to the previously-un-hinted mid-stream `chunk.error` throw.
	
	  **`ui/src/hooks/useChat.ts`** — the bigger structural change: `runTurn` was nested inside `sendMessage`'s closure, so nothing else could call it. Lifted to hook-body scope (a `useCallback`), with the shared turn-setup (abort controller, streaming state, lazy tools/env init) factored into a new `beginTurn` helper both `sendMessage` and the new `continueTurn` call. Added: a one-shot reactive retry (rolls the visible transcript back to pre-attempt state, retries once with a stricter budget, and prepends a visible "trimmed to fit" note on success — a `retryState` object threaded through the whole recursive call chain, not global, so it's one retry per top-level turn, not one per tool-call depth level) for when proactive trimming still isn't enough; and a real **Continue** mechanism for the `MAX_TOOL_ITERATIONS` cap — rejected auto-continue (unsafe given the two already-documented real looping incidents that motivated `AGENT_BEHAVIOR_HINT`) and raising the cap (doesn't fix anything structurally, works against the budget goal) in favor of a button matching the app's existing approval-gate philosophy: hitting the cap now stores the pre-notice message list (`pausedMessages`), and clicking Continue resumes `runTurn` from there with depth reset to 0 — a fresh full iteration budget, same as any other tool-call round already gets, still gated behind the normal per-call approval prompt.
	
	  **New Settings → Chat-window UI**: a `.chat-window__continue` band (same visual treatment as the tool-approval prompt) appears when paused, with a Continue button.
	
	  **Tests**: added a minimal `vitest` setup (this repo's `ui/` side had zero test infra before this — `npm test` now runs it) scoped to `contextBudget.ts`'s pure logic — 9 tests covering the no-op fast path, the per-message ceiling, oldest-first collapse ordering, the most-recent-user-message invariant, the toolCalls-preservation fix above, and system-message handling. One test failure caught during writing (not a implementation bug — a test whose budget math accidentally spilled from pass 2 into pass 3, asserting the wrong pass's output) was fixed by retuning the test's own numbers.
	
	  `npx vitest run` (9/9 pass), `tsc --noEmit`, `vite build`, and `cargo check` (Rust side untouched, confirmed unaffected) all clean.
	
	  **Live test found a real gap the same day, fixed the same day**: user hit "No response — the model likely ran out of context space" on a single `read_file` call at the *default* `numCtx=8192` — not an edge case. The retry logic above only fired on a *thrown* `OllamaError`, but this failure was a successful 200 OK stream containing only thinking tokens and no content at all — the model spent its whole remaining generation budget thinking and never reached an answer, a generation-side exhaustion, not a rejected request. Added `isEmptyThinkingOnly()` to detect this result shape (no thrown error to catch) and feed it into the *same* one-shot retry path as the thrown-error case, but with a harder cut (0.25× budget vs. 0.5× for a real error) since the goal here is specifically freeing generation headroom, not shrinking an oversized payload. **Honestly scoped, not oversold**: this measurably improves the odds and turns a silent dead end into one real retry attempt plus a clear terminal message, but a sufficiently long-thinking model could in principle still exhaust even a maximally-trimmed request at a given `numCtx` — there's no way to guarantee this never happens again from the input side alone. `tsc --noEmit`/`vitest`/`vite build` clean after the fix; not yet re-verified live #phase3 #harness
- [x] **Theme system evolution + Cody Duck theme + game-UI chrome pass.** User gathered external design feedback while iterating on themes separately, then asked to implement it: (1) grow theme packs from pure color skins into layered semantic tokens, (2) a real "Cody Duck" mascot theme from the logo's actual colors, (3) bake radius/font "shape identity" into the app rather than into packs, (4) add real font customization per pack (user's explicit ask, listing Google Fonts to draw from: Nunito Sans, M PLUS Rounded 1c, Zen Maru Gothic, Kosugi Maru, Quicksand, Fredoka) — a partial reversal of the earlier "palette-only" scope decision, scoped narrowly to just font rather than the full typography/radius/effects "appearance pack" alternative that was explicitly rejected.
	
	  **New semantic surface tokens** (`index.css`): `--sidebar-bg`, `--titlebar-bg`, `--input-bg`, `--hover-bg`, `--selected-bg`, `--focus-ring` — each defined as a *live* `var()` chain onto an existing foundation color (e.g. `--sidebar-bg: var(--bg-alt)`), not a static copy, so a pack that overrides `--bg-alt` but not `--sidebar-bg` still gets the right color automatically; a pack only needs to set one directly when it wants that surface to diverge from its default parent. Wired into real CSS, not left inert: `.titlebar`/`.sidebar` background + `:focus-visible`/input-focus outline color, replacing hardcoded `var(--bg-alt)`/`var(--accent-border)` references.
	
	  **Baked-in shape identity** (not per-pack, per the earlier scope decision): `--radius-sm/md/lg/pill` (8/14/20/999px) applied to buttons, inputs, and sidebar rows; `--sans` default stack changed to lead with rounder display fonts (`Quicksand`, `M PLUS Rounded 1c`, `Nunito Sans`) before falling back to `system-ui` — this is a *global* baseline shift, every theme inherits the friendlier type by default, not just Cody Duck. **Real font customization added on top**: `ThemePack` gained an optional `font` field (deliberately separate from the color-only `vars` map, since it isn't a color and needs its own validation — a new `FONT_RE` regex, same untrusted-input posture as the existing `COLOR_RE`/`ID_RE`, rejecting anything with `{ } ; < >` or backslashes before it can reach the injected `<style>` block). `applyThemePack` now emits `--sans: <pack.font>;` when set. **Honest limitation, not glossed over**: these Google Fonts aren't bundled with the app — this stays offline-first rather than fetching webfonts over the network for a cosmetic feature, so a listed font only actually renders if the user has it installed locally; otherwise the stack's fallbacks apply silently. Bundling real font files is a real follow-up if guaranteed rendering matters more than staying network-free.
	
	  **Cody Duck theme** (built by the user directly into `themes.ts` from their own gathered feedback, `font: 'M PLUS Rounded 1c'` added on top by this pass): navy/blue/yellow palette from `codychatbanner.png`, plus forward-compat brand-color vars (`yellow`/`orange`/`navy`/`blue`/`cream`) not wired to any component yet — inert today per `sanitizeThemePack`'s existing "unknown keys are skipped, not failed" design, available for a future component (e.g. a duck mascot SVG) to consume without another format change. **Psycho Duck stays exactly as the deliberately chaotic joke theme**, per the earlier logged decision — only addition: `font: 'Fredoka'` for an "extra mascot-y" joke-appropriate typeface.
	
	  **Chrome pass matching the "old because of where colors are used, not the hues" diagnosis**: titlebar and message-input got 2px accent-colored borders (was 1px neutral `--border`) instead of flat hairlines; sidebar rows are more rounded with the active row now a real card (fill + visible accent border, not just a background tint); the Send button is solid-filled (`background: var(--accent)`) instead of the previous pale accent-bg outline that read as disabled; the global scrollbar thumb is now accent-tinted (`color-mix` toward `var(--accent)`) instead of neutral gray, so every theme's scrollbar reads as branded, not just Cody Duck's.
	
	  `tsc --noEmit`, `vitest` (9/9, unaffected), and `vite build` all clean. Not yet visually verified live — user's own expectation going in was that Cody Duck specifically "needs more fine tuning," so a follow-up pass is expected once actually seen, not a surprise if it needs adjustment #phase3 #shell
- [x] **Cody Duck promoted to the base Light/Dark themes**, replacing the original generic purple-accent palette entirely. User had drafted a "Cody Duck (Dark)" companion pack directly (`cody-duck-dark-v2`), but it had drifted from actually being a dark-mode *companion* to light Cody Duck — different accent hue (yellow instead of blue, breaking continuity between light/dark), danger was orange (a brand color, not the red-family used for errors elsewhere), user-bubble was purple instead of the warm gold light-mode uses, and it was missing most of the semantic tokens (`sidebar-bg`/`titlebar-bg`/`focus-ring`/etc.) and brand constants (`yellow`/`orange`/`navy`/`blue`/`cream`) the light pack has. Rebuilt as a real companion: same blue accent family (brightened to `#5AA9FF` for dark-background contrast), same red-family danger, warm-gold `user-bubble` carried into dark mode as a translucent glow, full token parity with light, identical brand constants (mascot colors don't change between light/dark), same font. The `light`/`dark` pack ids kept their existing ids/names (just new `vars`+`font`) so anyone with "Light" or "Dark" already selected gets the new look automatically, no re-selection needed. The standalone `cody-duck`/`cody-duck-dark-v2` pack entries were removed as redundant once folded in — keeping them as separate list items alongside identically-colored "Light"/"Dark" entries would've just been confusing duplicates. **Also updated `index.css`'s plain `:root` and `prefers-color-scheme: dark` media block** (what **Auto** — the actual default with nothing selected — renders from) to hand-mirror the same values, so Auto and the explicit Light/Dark packs look identical rather than Auto quietly staying on the old purple palette while only the explicit picks got the new look. **Known drift risk, not a hidden one**: these are two independent copies (pack objects in `themes.ts`, plain CSS in `index.css`) that must be kept in sync by hand — already true of the pre-existing Light/Dark packs' relationship to `index.css`, not a new problem introduced here, but worth remembering next time either changes. `hextech`/"League Hextech" and Psycho Duck untouched. `tsc --noEmit`/`vitest` (9/9)/`vite build` clean. **User-confirmed live**: "otherwise it's great :)" #phase3 #shell
- [x] **League Hextech rebuilt** — user reported the sidebar color scheme looked broken and asked for a more authentically League-of-Legends-themed palette while at it. The previous version used an off-brand purple/orange combo that was never real Hextech, and relied entirely on the generic bg-alt/accent-bg fallback chain for the new semantic tokens rather than setting any explicitly. Rebuilt from the actual LoL client palette — deep navy `#0A1428`, Hextech gold `#C8AA6E` as the primary accent, the signature teal `#0AC8B9` repurposed as `success` — with every semantic surface token (`sidebar-bg`, `titlebar-bg`, `input-bg`, `hover-bg`, `selected-bg`, `focus-ring`) explicitly set rather than inherited, giving the sidebar its own distinct navy shade so it reads as a separated panel. `tsc --noEmit`/`vite build` clean; not yet visually re-verified live #phase3 #shell
- [x] **Tray-first window lifecycle**: close-to-tray, a compact Widget mode vs. a larger Full mode, and auto-hide-on-blur — the backlog item this closes, plus a concrete bug report on top ("clicking off the icon version just hides behind all windows and doesn't close down"). Read the actual pinned Tauri 2.11.5 source (`WindowEvent::CloseRequested`/`Focused`, `CloseRequestApi::prevent_close`, `Manager`/`Emitter` traits) before writing any code, same verify-don't-assume approach as the earlier Mica investigation.
	
	  **`ui/src-tauri/src/lib.rs`**: new `FullMode` state (parallel to the existing `AlwaysOnTop` pattern) tracks which size mode the window is in — drives whether focus-loss should auto-hide (Widget: yes, matching a typical tray popup like Windows' own Calendar flyout; Full: no, acts like a normal app window). A `WindowEvent` handler registered in `setup()` intercepts `CloseRequested` (prevents the real close, hides to tray instead — covers the custom titlebar's new ✕ button's underlying command *and* any other close path like Alt+F4) and `Focused(false)` (auto-hides only when not in Full mode — this is the actual fix for the reported bug: the window losing focus previously did nothing, so it just sat there behind whatever else got focus instead of properly hiding). Three new commands: `close_to_tray`, `expand_window` (grows to half the monitor's work area, centered — not corner-anchored, since a big window pinned to the corner would read as off-center for something meant to feel like the main app), `collapse_window` (shrinks back to Widget size, corner-anchored). Every "reopen from tray" path (left-click, Show/Hide menu, Pop to Corner menu) converges on a new `show_as_widget()` so they all give the same predictable starting state — always the compact widget, never resuming whatever size was last active — and emits a `window-shown-as-widget` event so the frontend can stay in sync.
	
	  **`ui/src/App.tsx`**: new ✕ (close-to-tray) and ⤢/⤡ (expand/collapse) titlebar buttons; listens for `window-shown-as-widget` to reset its local expand/collapse UI state when the window reopens from tray, rather than assuming. **`ChatWindow.tsx`** gained a `compact` prop (hides the model-picker/settings header entirely in Widget mode, where it doesn't fit and isn't the point of that surface).
	
	  **First live test caught two real bugs, both fixed the same session — not left for a future pass**: (1) the widget size was originally set via `tauri::PhysicalSize`, which on a DPI-scaled display shrinks the *actual usable CSS pixel area* well below the intended 420×620 (e.g. 420 physical px at 150% scaling is only ~280 CSS px) — this is exactly why the model picker and message input were visibly overflowing the window edge in the first screenshot. Fixed by switching to `tauri::LogicalSize`, which the platform scales automatically; `full_size()` deliberately stays `PhysicalSize` since it's derived from the monitor's own physical work area and doesn't have this problem. (2) The model-picker header didn't just need to be smaller — at true widget width it still didn't fit and needed to not render there at all, hence the new `compact` prop rather than just fixing the sizing bug alone.
	
	  `cargo check`, `tsc --noEmit`, `vitest` (9/9), `vite build` all clean. **Native window behavior can't be verified any other way than actually running the app** — same limitation as every past window/Mica investigation this session; needs a real rebuild + click-through, listed in To Test #phase3 #shell
- [x] **Real bug found and fixed: `ModelPicker.tsx` was silently, permanently overwriting a conversation's saved model.** User-reported (via a new personal scratchpad, `docs/codyfinds.md`, before formalizing to Kanban): a previously-saved custom model "doesn't find itself" and the conversation "lost all its context settings and system prompt". Root cause, confirmed by reading the code directly: every time the model list loaded, `if (!list.some(m => m.name === value)) onChange(list[0].name)` — if the conversation's saved model wasn't in that particular fetch (a custom model lagging behind a fresh `/api/tags` call, or genuinely not present), the picker silently swapped `conversation.model` to whatever happened to be first in the list, with zero confirmation or visibility, and persisted that swap immediately via the normal `updateConversation` path. This fully explains both reported symptoms as one root cause rather than two separate bugs: the model got silently swapped away, and since the *original* custom model's system prompt was baked in server-side via `/api/create` (not stored in the conversation's own separate `systemPrompt` field, which was never actually touched), switching to a different underlying model looks exactly like "it lost its system prompt" even though the conversation's own settings were intact the whole time. Fixed by removing the auto-swap entirely: a saved model not found in the current list now renders as its own distinct `⚠ {name} (not found)` option (warning-tinted picker border/text via a new `.model-picker--missing` class, with a fallback to `--danger` for packs that don't define `--warning`) so the user can see what happened and choose explicitly, instead of the app silently discarding their selection. `tsc --noEmit`/`vite build` clean; not yet re-verified live with the actual model that was originally lost #phase3 #shell
- [x] **Real Windows toast notifications with inline Approve/Deny buttons.** User asked for notifications specifically so a pending tool-call approval while the widget is hidden/minimized isn't invisible, and explicitly wanted the actual buttons on the notification itself ("would be nice just to hit approve button or deny") rather than a click-to-reopen-the-widget notification, since the in-app approval UI still feels "quite big" for that. **A real finding, checked before building on it rather than assumed**: the official `tauri-plugin-notification`'s action-button API (`registerActionTypes`/`onAction`, `Options.actionTypeId`) is advertised in its shared JS type definitions as cross-platform, but reading the actual 2.3.3 Rust source showed `register_action_types` is implemented *only* in `mobile.rs` — the desktop `show()` path never reads `action_type_id` from the notification data at all, so on Windows/macOS/Linux the action-button system is a silent no-op despite the JS API accepting it. Also checked the lower-level `notify-rust` crate this plugin depends on for desktop and confirmed its Windows backend *does* genuinely wire `.action()` calls to real WinRT toast buttons via `winrt-notification`'s `on_activated` callback — so real inline buttons are achievable, just not through the official plugin's advertised mechanism. Given the finding, asked the user whether to ship the simpler (but not what they'd actually asked for) click-to-open version first, or build the real thing; they chose the real thing.
	
	  **`ui/src-tauri/Cargo.toml`**: added `notify-rust` as an explicit dependency (not just transitively through the plugin) specifically for the tool-approval case; kept `tauri-plugin-notification` too, for the plain "response ready" case, which never needed actions in the first place — each library used for what it's actually good at rather than picking one and forcing both cases through it.
	
	  **`ui/src-tauri/src/lib.rs`**: new `notify_pending_approval` command builds a real `notify_rust::Notification` with `.action("approve", "Approve")`/`.action("deny", "Deny")`, shown on its own detached thread (`wait_for_action` blocks until the user interacts, so it can't run on the async command handler) — safe to always spawn a fresh thread per call since the app only ever has one tool call pending approval at a time. On an explicit Approve/Deny click, emits a `tool-approval-action` event with the action id; anything else (plain click, dismissal, timeout) all collapse to the same unlabeled response in `notify-rust`'s Windows backend with no way to distinguish them, so those are deliberately no-ops rather than guessed at. Caught the exact same camelCase/snake_case Tauri argument-matching bug already logged once this session (`edit_file`) before it could bite again — `tool_name`/`args_summary` needed `#[tauri::command(rename_all = "snake_case")]`, added proactively this time rather than found live.
	
	  **`ui/src/hooks/useChat.ts`**: a new `notifyIfHidden` helper checks `getCurrentWindow().isFocused()` before doing anything — no point interrupting with a notification for something already on screen. Called from `requestApproval` (tool-approval case, invokes the new Rust command) and from the turn-completion path when a final answer has content (plain case, uses the official plugin's `sendNotification`, with the standard `isPermissionGranted`/`requestPermission` check first). Wrapped in try/catch — a notification failing is a courtesy lost, not a reason to break the actual chat/approval flow.
	
	  **`ui/src/App.tsx`**: new `tool-approval-action` listener (same pattern as the existing `window-shown-as-widget` one) routes straight to the existing `approveToolCall`/`denyToolCall` — no special-casing between "approved via the OS notification" and "approved via the in-app button," both just resolve whatever's currently pending. This works because close-to-tray only ever *hides* the window rather than destroying it, so the React state (and the pending approval's promise resolver) stays alive in the background the whole time.
	
	  `notification:default` added to `src-tauri/capabilities/default.json`. `cargo check`, `tsc --noEmit`, `vitest` (9/9, unaffected), `vite build` all clean. **Nothing here has been visually confirmed live** — this is real OS toast + native action-button territory, which by definition can't be verified any way other than actually seeing a toast appear and clicking a button on it #phase3 #shell #harness
- [x] **Per-conversation settings moved into Settings → General, under a new "Active conversation" section.** User's own motivation: actively testing the "save as custom model" flow to manage different personas/modelfiles and system prompts, and wanted that consolidated with the rest of app settings rather than behind a collapsible toggle buried in the chat header — especially now that the widget/full-mode split means the chat header doesn't even render in compact mode.
	
	  `ChatWindow.tsx` lost `SettingsPanel`, its `settingsOpen` state, and the "Settings" toggle button entirely — the header now shows only the `ModelPicker` (a frequent quick-switch action, kept separate from the "settings" concept) when not compact. `modelListRefreshKey` (bumping the picker's list after a new custom model is saved) got lifted from local `ChatWindow` state up to `App.tsx`, since the save action and the picker that needs to react to it are now in different components entirely. `SettingsMenu.tsx`'s General tab embeds `SettingsPanel` directly under a new "Active conversation" subheading, operating on whichever conversation is currently active (`App.tsx` wires `active?.model`/`active?.systemPrompt`/`active?.params` with safe fallbacks for the no-conversation-selected case). `SettingsPanel`'s old "Connected to X — change the base URL in Settings → General" hint was removed since it would've become nonsensically self-referential sitting directly inside Settings → General, right next to the actual base-URL field.
	
	  **User screenshot-confirmed the move itself works correctly** (model info badges, system prompt, save-as-model, all three sliders render under General as expected), and flagged a real follow-up from that same screenshot: `.settings-menu__section`'s `max-width` was a fixed 480px — fine for the narrow widget, but left most of the pane empty in the much wider full-mode window. Widened to `min(720px, 100%)`, applying to every tab (Theme/General/Tools) since they all share the one class, not just General. `tsc --noEmit`/`vitest` (9/9)/`vite build` clean; the width fix itself not yet re-verified live #phase3 #shell
- [x] **Split message-bubble text color** (`--user-bubble-text`/`--assistant-bubble-text` in `index.css`, both defaulting to the existing `--text-h` via the same live-fallback-chain pattern as every other semantic token added this session) — user hit this while editing the Dark pack's `user-bubble` directly (changed it to a near-white fill to differentiate from the dark assistant bubble) and noticed the shared `--text-h`, tuned for dark surfaces, went nearly illegible against their new light one. Set Dark's own `user-bubble-text` to `#0B2A5B` (reusing Light's own heading color, since the bubble is now effectively light-mode-colored even though the rest of the theme stays dark) to actually fix the reported case. `.message__content` now reads from `var(--assistant-bubble-text)`/`var(--user-bubble-text)` depending on which bubble it's in, instead of one shared `--text-h` for both #phase3 #shell
- [x] **Model selection added to Settings → General.** User-reported gap: the only place to switch a conversation's model was the header `ModelPicker`, which doesn't render at all in compact/Widget mode (hidden by design, see the earlier window-lifecycle work) — so there was genuinely no way to change models while in Widget mode. `SettingsPanel` now embeds the same `ModelPicker` component (not a second implementation) as its first field, wired to a new `onModelChange` prop threaded through `SettingsMenu` → `App.tsx` (`updateConversation(active.id, { model })}`, same pattern as the system-prompt/params fields already there). Also threaded `modelListRefreshKey` through so saving a custom model refreshes this picker's fetched list too, not just the header one. Added `.settings-panel__field .model-picker { width: 100% }` so it stretches to match the other form fields instead of shrink-wrapping. `tsc --noEmit`/`vitest` (9/9)/`vite build` clean; not yet re-verified live specifically in Widget mode #phase3 #shell
- [x] **Real bug fix, distinct from the earlier ModelPicker fix: a custom model's baked-in system prompt was silently overridden on every single request.** User-reported live (2026-08-16, recurrence of the earlier "assistant lost its system prompt" report): confirmed **not** the already-fixed ModelPicker auto-swap bug — a genuinely separate root cause. `useChat.ts` always sends an explicit `system` role message on the wire, because `AGENT_BEHAVIOR_HINT` (the standing "converge quickly" instruction) is a non-empty constant folded into `systemParts` unconditionally — so `systemContent` is never falsy, regardless of whether the conversation has its own `systemPrompt` set. Ollama's `/api/chat` **replaces** a Modelfile's baked-in `SYSTEM` prompt whenever the request supplies its own `system` message — it does not merge them. So every turn against a custom model saved via "Save as custom model" (`/api/create`) silently erased that model's own persona the instant its per-conversation `systemPrompt` was empty (i.e. any new conversation, since new conversations always start with `systemPrompt: ''`). **Verified against the real `assistant:latest` model, not assumed**: `curl -X POST /api/show -d '{"model":"assistant:latest"}'` confirmed `data.system` really does hold the full baked-in persona text. Fixed: `ModelInfo` (`lib/ollama.ts`) gained a `system: string` field parsed from `/api/show`; `useChat.ts` added `modelSystemCacheRef` (a `Map<model, system>`, populated lazily per-model in `beginTurn` alongside the existing tools/env-context lazy init) and folds the cached value into `systemParts` ahead of the per-conversation prompt, so both now layer together instead of one silently replacing the other. Cache invalidated via a new `invalidateModelSystemCache()` (wired into `onModelCreated` in `App.tsx`) so re-saving over an existing custom model's name doesn't keep serving a stale cached prompt for the rest of the session. `tsc --noEmit`/`vitest` (9/9)/`vite build` clean; the fetch-and-fold path itself not yet re-verified live against a real multi-turn conversation on `assistant:latest` #phase3 #harness
- [x] **Real bug fix: new chats defaulted to a hardcoded, never-actually-pulled model.** User-reported live: every new conversation opened already showing "⚠ llama3.2 (not found)". Root cause: `App.tsx`'s `DEFAULT_MODEL = 'llama3.2'` — a leftover placeholder from before this app fetched real model lists, never updated once `ModelPicker`'s missing-model warning UI existed (that fix, from earlier this session, correctly stopped silently swapping to `list[0]`, but that meant a bad default now shows the warning instead of being masked). Fixed by removing the hardcoded name: `useConversations.ts::createConversation` now defaults new chats to whichever model the most recent conversation used (`conversations[0]?.model`, list is kept newest-first) rather than any fixed string — matches how most chat apps remember your last pick. `DEFAULT_MODEL` is now `''`, only used for the very first conversation ever, before any model has been picked; `ModelPicker` already degrades cleanly for an empty value (shows "loading…", no false "not found" badge). `tsc --noEmit`/`vitest` (9/9)/`vite build` clean #phase3 #shell
- [x] **MCP env-var support + curated "Suggested servers" quick-fill.** Motivated by researching real MCP servers relevant to the user's own tools (Obsidian, Blender) — `mcp-obsidian` turned out to require three env vars (`OBSIDIAN_API_KEY`/`HOST`/`PORT`) to auth against Obsidian's Local REST API plugin, and this app's MCP client had **no env-var support at all**: `McpServerConfig` was command+args only, `spawn_and_handshake` in `mcp.rs` never called `.envs(...)`. A real, silent gap — any auth-requiring server (Obsidian, GitHub's official server with a PAT, etc.) simply couldn't have worked. Fixed: `McpServerConfig` gained an optional `env?: Record<string,string>`; `mcp_connect` (Rust) gained an `env: HashMap<String,String>` param passed straight to `Command::envs`; `McpServers.tsx` gained a "KEY=VALUE per line" textarea, parsed the same naive-but-sufficient way args already are, only included on the wire if non-empty. Also added a small **hand-curated "Suggested" chip row** (Filesystem/Blender/Obsidian — verified via direct research, not guessed) that prefills the add-server form (name/command/args/env) rather than connecting immediately, since Filesystem needs a real path and Obsidian needs a real API key only the user has — matches the existing manual-connect-only posture exactly. Existing server rows show an env-var *count*, not the raw values, to avoid dumping secrets on screen. `cargo check`/`cargo test` (11 tests)/`tsc --noEmit`/`vite build` all clean; the new env-var plumbing and suggestion chips not yet exercised live against a real server #phase4 #harness #shell
- [x] **`.message__role` fix + real font-override control.** Two follow-ups from a live screenshot. (1) The "USER"/"ASSISTANT" label above each bubble was missed in the bubble-text-color split just above — it had no explicit `color` at all, inheriting the generic body `--text` regardless of which bubble it sat on; screenshot showed it reading as a faint, hard-to-read gray against the light user bubble. Fixed with the same `.message--user`-scoped override pattern already established for `.message__content`. (2) User noticed the requested Google Fonts (Quicksand, M PLUS Rounded 1c) weren't actually rendering — the already-documented "not bundled, only renders if installed locally" limitation, but user correctly pushed back that a silent fallback with zero visibility isn't good enough; wanted real control in the UI, not just a caveat in a comment.
	
	  Built a manual font override, deliberately **separate from theme packs entirely** (not per-pack) since "the fonts packs ask for aren't installed on this machine" isn't a per-pack problem — it survives switching packs rather than resetting each time. New `loadFontOverride`/`saveFontOverride`/`applyFontOverride` in `lib/themes.ts` (`ollama-ui:font-override` in localStorage, validated through the same `FONT_RE` already used for a pack's own `font` field). Applied as an **inline style on `<html>`** rather than through the pack-injection `<style>` mechanism — inline styles beat any stylesheet rule regardless of specificity, so the override keeps winning even as `applyThemePack` swaps in a different pack's injected CSS on every theme switch, with zero interference between the two mechanisms. New Settings → Theme UI: a text input plus a **live preview rendered directly in whatever's typed** (not the app's own font) — an honest test of whether a given font is actually available on this machine, not just the same fallback either way — with a "Reset to theme default" button that appears once an override is set.
	
	  `tsc --noEmit`, `vitest` (9/9, unaffected), `vite build` all clean. Neither fix re-verified live yet #phase3 #shell




%% kanban:settings
```
{"kanban-plugin":"board","list-collapse":[null,null,null]}
```
%%